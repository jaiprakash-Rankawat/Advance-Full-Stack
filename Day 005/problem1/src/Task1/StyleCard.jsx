import React from "react";

const StyleCard = () => {
  return (
    <div
      style={{
        backgroundColor: "lightblue",
        padding: "20px",
        borderRadius: "10px",
        color: "darkblue",
      }}
    >
      <h1>InLine Style</h1>
      <p>Inline Style is applied using style attribute inside the tag</p>
    </div>
  );
};

export default StyleCard;
